<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect all requests to the api/public directory
    RewriteCond %{REQUEST_URI} !^/api/public
    RewriteRule ^(.*)$ /api/public/$1 [L]
</IfModule>